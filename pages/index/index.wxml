<view class="main">
    <map id="myMap" class="map" longitude="{{longitude}}" latitude="{{latitude}}" controls="{{controls}}" show-location="true" scale="15" bindcontroltap="controltap" markers="{{markers}}" bindregionchange="regionchange">
    </map>
</view>